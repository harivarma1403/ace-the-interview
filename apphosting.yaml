# Firebase App Hosting configuration file
# Docs: https://firebase.google.com/docs/app-hosting/configure

runtime: nodejs20
entry: nextjs

runConfig:
  # Optional: controls how many server instances can be spawned
  maxInstances: 1
